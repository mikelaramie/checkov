# Same fixtures as GCPTaggableAlternateLabels; checks labels.env (suffix mapping).
metadata:
  name: "Ensure GCP taggable resources have an env label"
  id: "CUSTOM_GCP_TAGGABLE_LABEL_KEYS"
  category: "GENERAL_SECURITY"
scope:
  provider: gcp
definition:
  cond_type: "attribute"
  resource_types: "taggable"
  attribute: "labels.env"
  operator: "exists"
